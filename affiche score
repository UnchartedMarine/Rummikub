int main(char nom)
{
    FILE* fichier = NULL;
    char caractereActuel ;
    int score;
 
    fichier = fopen("test.txt", "r");
 
    if (fichier != NULL)
    {
        while(caractereActuel !=nom)
        {
            fscanf(fichier, "%c;%d", &char, &score);
        }
        printf("%d", score);
        fclose(fichier);
    }
 
    return res;
}
