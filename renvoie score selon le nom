int score(char nom)
{
    FILE* fichier = NULL;
    char caractereActuel ;
    int score;
 
    fichier = fopen("tab_score", "r");
 
    if (fichier != NULL)
    {
        while(caractereActuel !=nom)
        {
            fscanf(fichier, "%s;%d", &caractereActuel, &score);
        }
        printf("%d", score);
        fclose(fichier);
    }
 
    return res;
}
