int main(int score, char nom)
{
    FILE fichier = NULL;
 
    fichier = fopen("tab_score", "w");
 
    if (fichier != NULL)
    {
        fputs("nom;score", fichier);
        fclose(fichier);
    }
 
    return 0;
}
